extends ../layout

block content
 button
  a.navbar-brand(href="/category/add")= Site.Title
 #categorieslist.content.hidden
        .media(v-for="t in categories")
            //a.pull-left(href='/category/{{t._id}}')
                .media-object(v-thumbimage='t')
            div.media-body
                h4.media-heading
                    a(href='/category/{{t._id}}') {{t.title}}
                p {{{t.summary}}}
                p
                    span.timeSpan {{t.updated_at}}

        {{{pager}}}
        
block script
    include list_js.jade
